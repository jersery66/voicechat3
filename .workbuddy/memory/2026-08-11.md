# 2026-08-11 工作日志

## 任务：从头到脚审查 voicechat3 项目代码，检查问题
- 来源：用户导出两个旧会话文件（继续任务_2026-08-11_21-32.md、了解_voicechat3_项目全过程_2026-08-11_23-52.md），要求"继续任务"。原任务即通读项目、检查代码问题。
- 项目：小薇(心医生) AI 心理咨询语音系统，本地目录 `E:\数据库\代码\Data\PDCH\voicechat`，约 95 个 .py。
- 做法：分 5 个 Explore 子代理并行深度审查（核心对话管线、状态机/报告、STT/TTS/视频/情绪、UI 层、游戏/适配器/app/脚本），对照 AGENTS.md 的 10 条关键约束逐条核实。我自己精读了 config.py。
- 产出：整合审查报告 `docs/代码审查报告_2026-08-11.md`。
- 关键发现（去重后）：高 16 / 中 29 / 低 20。
  - 最高危：H1 受试者编号未净化→路径穿越；H2 模型判定结束在工作线程弹模态框崩溃；H3 ControlPanel 缺 reset_form 导致换人崩溃；H5 symptom_scores 仅一条路径重置→量表连环触发；H6 _history_visible_text 不处理反转/不剥内部词→历史污染；H11 TTS 无重复加载/卸载保护；H12 download_media 未定义 MUSIC_DIR 必崩；H13 preprocess 空文件 IndexError+就地覆盖数据丢失。
  - 约束缺口：约束8 停止序列被注释未下发（M7）；约束7 内部词含过宽"防御"误杀正常回复（M8）；约束3 量表启动轮次门禁 MIN_ROUNDS_BEFORE_SCALE 导入未用（M9）。
  - 重构模块：app/engine.py 仅影子模式且与 pipeline 零集成（H14/H15/M6）；adapters/ 孤立未接线（H16 协议签名不一致）。
- 报告已 present 给用户。未修改任何源文件（审查阶段）。后续若用户要求修复，按 P0→P1→P2 优先级。

## 任务：修复 P0 高危问题（用户指令"修"）
- 按要求动手修了 P0 批高危，全部已 `py_compile` 通过。修改清单：
  - H1 路径穿越：`data/data_manager.py` set_user_id 加白名单净化（re 替换非 [A-Za-z0-9_-] 为 _，截断64）；`services/report_generator.py` 报告 PDF 文件名同样 `_safe_subject_id` 净化。
  - H3 换人崩溃：`ui/control_panel.py` 新增 `reset_form()`（清空 id/age、combo 复位、按钮/label 复位、停闪烁/录音），`_prepare_next_subject` 调用不再 AttributeError。
  - H5 量表连环触发：`services/pipeline.py` 在 agent `start` 分支与确定性兜底成功启动量表时，也重置 `symptom_scores`/`symptom_turns`。
  - M9 量表门禁：`services/pipeline.py` agent `start` 分支加 `current_rounds >= MIN_ROUNDS_BEFORE_SCALE` 守卫。
  - M7 停止序列：`services/llm_service.py` 恢复 `stop: ["User:","Visitor:","用户:","来访者:","Human:"]` 下发 Ollama。
  - M8 内部词误杀：`core/tags.py` 从 `_FORBIDDEN_INTERNAL_TERMS` 移除过宽的"防御"/"具体化"（保留"高/中/低防御"等歧义短语），加注释说明为何保持精确。
  - H12 MUSIC_DIR 崩溃：`scripts/download_media.py` 新增 `MUSIC_DIR = MEDIA_DIR / "music"`，并把 `RELAXATION_DIR` 从 `PROJECT_ROOT.parent` 改为 `MEDIA_DIR/relaxation`（与 video_tool.FILE_MAP 查找根一致），避免下载到父目录且运行时找不到。
  - H13 知识库预处理崩溃/丢数据：`scripts/preprocess_knowledge_base.py` 每文件处理前 `_backup_file` 备份、失败 `_restore_backup` 还原；打印示例前判空 `if fixed:`。
- 未触碰：H2 线程弹框、H6 历史污染、H11 TTS 显存、H14/H15/H16 引擎契约——这批归 P1，用户未要求先修，等下一步。
- 注：本仓库测试用 `python -m pytest tests/`，py_compile 仅语法校验，未跑测试套件（未确认 pytest 依赖是否就绪）。
