# Optional GPU/model dependencies for STT/TTS local inference.
# Install after requirements-core.txt on the target machine that owns CUDA.
torch
torchaudio
funasr>=1.3
modelscope>=1.20
accelerate>=1.0
transformers>=4.40
sentencepiece>=0.2
librosa>=0.10
# Legacy CosyVoice only. The active VoxCPM2 backend uses sounddevice instead;
# keeping PyAudio out avoids a Windows build-time dependency on PortAudio.
voxcpm>=2.0
